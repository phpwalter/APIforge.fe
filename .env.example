# Base URL for the APIforge API server (no trailing slash).
# Copy this file to .env.local (or .env) and fill in the real value —
# .env* files besides this example are gitignored.
VITE_API_SERVER=http://localhost:3000
